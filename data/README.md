# Data

This folder holds the core CSV tables for the relational dataset.

- `game.csv` — games entity (header row only until populated).
- `line_score.csv` — line score / scoring by period (header row only until populated).
- `team_history.csv` — team history dimension (header row only until populated).
- `other_stats.csv` — supplemental stats (header row only until populated).

